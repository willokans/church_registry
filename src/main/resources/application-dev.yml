spring:
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    com.example.registry: DEBUG
    org.springframework.web: DEBUG

app:
  openapi:
    enabled: true

